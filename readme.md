# Scale Managemen System

## Git cheat

#### 1. Apply .gitignore on an existing repository already tracking large number of files
    git rm -r --cached .

## Development setup

#### 1. Database (SQL Server)

TODO - set up SQL server
